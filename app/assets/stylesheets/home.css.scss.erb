// Place all the styles related to the home controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/

@mixin transition($transition-property, $transition-time, $method) {
    -webkit-transition: $transition-property $transition-time $method;
    -moz-transition: $transition-property $transition-time $method;
    -ms-transition: $transition-property $transition-time $method;
    -o-transition: $transition-property $transition-time $method;
    transition: $transition-property $transition-time $method;
}


/*
 * Globals
 */

.background {
  background-color: white;
  width: 100%;
}

/* MARKETING CONTENT
-------------------------------------------------- */

/* Pad the edges of the mobile views a bit */
.marketing {
  padding-right: 15px;
  padding-left: 15px;
}

/* Center align the text within the three columns below the carousel */
.marketing .col-lg-4 {
  margin-bottom: 20px;
  text-align: center;
}
.marketing h2 {
  font-weight: normal;
}
.marketing .col-lg-4 p {
  margin-right: 10px;
  margin-left: 10px;
}

/* Links */
a,
a:focus,
a:hover {
  color: #fff;
}

/* Custom default button */
.btn-default,
.btn-default:hover,
.btn-default:focus {
  color: #333;
  text-shadow: none; /* Prevent inheritence from `body` */
  background-color: #fff;
  border: 1px solid #fff;
}

/* Featurettes
------------------------- */

.featurette-divider {
  margin: 80px 0; /* Space out the Bootstrap <hr> more */
}

/* Thin out the marketing headings */
.featurette-heading {
  font-weight: 300;
  line-height: 1;
  letter-spacing: -1px;
}


/*
 * Base structure
 */

html,
body {
  height: 100%;
}
body {
  color: #5a5a5a;
  background: url(<%= image_path('city.jpg') %>) no-repeat center center fixed; 
  -webkit-background-size: cover;
  -moz-background-size: cover;
  -o-background-size: cover;
  background-size: cover;
  -webkit-background-size: length_x length_y;
}

/* Extra markup and styles for table-esque vertical and horizontal centering */
.site-wrapper {
  display: table;
  width: 100%;
  height: 100%; /* For at least Firefox */
  min-height: 100%;
}
.site-wrapper-inner {
  display: table-cell;
  vertical-align: top;
}
.cover-container {
  margin-right: auto;
  margin-left: auto;
}

/* Padding for spacing */
.inner {
  padding: 30px;
  text-align: center;
}
.row {
  margin-top: 60px;
  margin-bottom: 60px;
}
.img-responsive {
  max-height: 400px;
}

.quick-slogan {
  text-align: center;
  color: white;
  width: 100%;
  background-image: radial-gradient(rgba(0,0,0,.5),rgba(0,0,0,0));
}
.featurette-text{
  margin-top: 70px;
}
.featurette-image {
  margin-left: auto;
  margin-right: auto;
}


/*
 * Header
 */
.masthead-brand {
  margin-top: -200px;
  margin-bottom: 10px;
  @include transition(all, .5s, cubic-bezier(0.175, 0.885, 0.32, 1.275));
  img {
    height: 100px;
    width: 100px;
  }
}

.masthead-nav > li {
  display: inline-block;
}
.masthead-nav > li + li {
  margin-left: 20px;
}
.masthead-nav > li > a {
  padding-right: 0;
  padding-left: 0;
  font-size: 16px;
  font-weight: bold;
  color: #fff; /* IE8 proofing */
  color: rgba(255,255,255,.75);
  border-bottom: 2px solid transparent;
}
.masthead-nav > li > a:hover,
.masthead-nav > li > a:focus {
  background-color: transparent;
  border-bottom-color: rgba(255,255,255,.25);
}
.masthead-nav > .active > a,
.masthead-nav > .active > a:hover,
.masthead-nav > .active > a:focus {
  color: #fff;
  border-bottom-color: #fff;
}

@media (min-width: 768px) {
  .masthead-brand {
    float: left;
  }
  .masthead-nav {
    float: right;
    margin-top: -100px;
    opacity: 0;
    @include transition(all, .5s, cubic-bezier(0.215, 0.61, 0.355, 1));
  }
}


/*
 * Cover
 */

.cover {
  padding: 0 20px;
  text-align: center;
  text-shadow: 0 0px 5px rgba(0,0,0,1);
  color:#fff;
  background-image: radial-gradient(circle closest-side, rgba(0,0,0,.8),rgba(0,0,0,0));
}
.cover .btn-lg {
  padding: 10px 20px;
  font-weight: bold;
}

.img-circle {
  width: 100%;
}


/*
 * Footer
 */

.footer {
  background-color: #FFF;
  width: 100%;
}

.social {
  width: 600px;
  padding-left: 0;
}
.social li {
  display: inline;
  list-style-type: none;
  text-align: center;
  img {
    width: 100px;
    margin-top: 0px;
    opacity: 0.6;
    @include transition(all, .3s, linear);
  }
  img:hover {
    width: 100px;
    opacity: 1;
    margin-top: -15px;
  }
}

#email-feedback {
  max-width: 600px;
  margin-right: auto;
  margin-left: auto;
}

.form-container {
  @include transition(all, .5s, linear);
}

/*
 * Affix and center
 */

@media (min-width: 768px) {
  /* Pull out the header and footer */
  .masthead {
    position: absolute;
    top: 0;
  }
  .mastfoot {
    position: absolute;
    bottom: 0;
  }
  /* Start the vertical centering */
  .site-wrapper-inner {
    vertical-align: middle;
  }
  /* Handle the widths */
  .masthead,
  .mastfoot,
  .cover-container {
    width: 100%; /* Must be percentage or pixels for horizontal alignment */
  }
  .cover {
    padding: 200px;
  }
}

@media (min-width: 992px) {
  .masthead,
  .mastfoot,
  .cover-container {
    width: 900px;
  }
}

#market1, #market2, #market3 {
  opacity: 0;
  @include transition(all, 2s, cubic-bezier(0.215, 0.61, 0.355, 1));
}
#main-cover {
  opacity: 0;
  @include transition(all, 2s, cubic-bezier(0.215, 0.61, 0.355, 1));
}
#mock1 {
  opacity: 0;
  margin-right: -50px;
  @include transition(all, 1s, cubic-bezier(0.215, 0.61, 0.355, 1));
}
#mock1-text {
  opacity: 0;
  @include transition(all, 1s, cubic-bezier(0.215, 0.61, 0.355, 1));
}
#mock2 {
  opacity: 0;
  margin-left: -50px;
  @include transition(all, 1s, cubic-bezier(0.215, 0.61, 0.355, 1));
}
#mock2-text {
  opacity: 0;
  @include transition(all, 1s, cubic-bezier(0.215, 0.61, 0.355, 1));
}
.font-anim {
  margin-left: -300px;
  opacity: 0;
  @include transition(all, 1s, cubic-bezier(0.175, 0.885, 0.32, 1.275));
}
